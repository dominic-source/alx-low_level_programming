#include "main.h"

/**
 * mul - multiply two integers
 * Description: A function to multiply two integers
 * Return: 1 or 0
 * @a: a is the first integer argument
 * @b: b is the second integer argument
 */
int mul(int a, int b)
{
long result = a * b;
return (result);
}
