#include <stdio.h>

/**
 * main - Outputs a string
 * @void: No arguments is needed
 * Description: Outputs a string using prinf
 * Return: This function return zero
 */
int main(void)
{
char str[] = "with proper grammar, but the outcome is a piece of art,";

printf("%s", str);
return (0);
}
