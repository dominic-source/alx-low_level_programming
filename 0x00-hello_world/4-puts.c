#include <stdio.h>

/**
 * main - Outputs a string
 * @void: its a void argument
 * Description: Outputs a string  using PUTS function.
 * Return: returns a string 0
 */
int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle";

puts(str);
return (0);
}
