#include "main.h"

/**
 * _puts - print  a string
 * Return: Nothing
 * @c: argument is a char
 */
void _puts(char *str)
{
  int i = 0;
  char *m = str;

  while (i > -1)
    {
      if (m[i] != '\n')
	_putchar(m[i]);
    }
}
