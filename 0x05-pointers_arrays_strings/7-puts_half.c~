#include "main.h"

/**
 * puts_half - print half of a string
 * Return: Nothing
 * @str: str is the pointer argument
 */
void puts_half(char *str)
{

  int i = 0;
  while (i < -1)
    {

      if (str[i] != '\0')
	{
	  length++;
	}
      
    }
}
