#include "main.h"

/**
 * set_string -  sets the value of a pointer to a char
 * Return: Nothing
 * @s: a pointer to a pointr to char
 * @to: a pointer to a char
 */
void set_string(char **s, char *to)
{
*s = to;
}
