#include "lists.h"

/**
 * free_listint - frees a list of listint_t
 * @head: head to free
 */
void free_listint(listint_t *head)
{

	listint_t *f;

	f = head;
	while (f->next != NULL)
	{
		free(f);
		f = f->next;
	}
	free(f);
}
