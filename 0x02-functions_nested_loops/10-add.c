#include "main.h"

/**
 * add - Adds two integers
 * Descreption: Add function adds two integers and produces a result
 * Return: result of addition
 * @n: this argument is an integer and is required
 * @m: this argument is an integer and is required
 */
int add(int n, int m)
{
int result;
result = n + m;

return (result);
}
