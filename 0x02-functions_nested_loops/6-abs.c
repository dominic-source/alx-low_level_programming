#include "main.h"

/**
 * _abs - Prints absolute value of an integer
 * Description: This function will print out the absolute value of an integer
 * @c: c is an integer
 * Return: 0
 */
int _abs(int c)
{
if (c < 0)
return (c * -1);
else
return (c);
}
