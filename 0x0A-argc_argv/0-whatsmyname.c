#include <stdio.h>

/**
 * main - a function to print arguments
 * Return: 0
 * @argc : argument count
 * @argv: arguments
 */
int main(int argc, char *argv[])
{
if (argc >= 1)
printf("%s\n", argv[0]);
return (0);
}
